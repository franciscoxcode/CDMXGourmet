<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CDMXGourmet">
        <activity
            android:name=".EditDishActivity"
            android:exported="false" />
        <!-- ✅ Correct AddDishActivity (root package) -->
        <activity
            android:name=".AddDishActivity"
            android:exported="false" /> <!-- 🏠 Main launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 🍽 Restaurant detail activity -->
        <activity
            android:name=".activities.RestaurantDetailActivity"
            android:exported="false" /> <!-- 📋 Dish detail activity -->
        <activity
            android:name=".activities.DishDetailActivity"
            android:exported="false" /> <!-- ➕ Add restaurant activity -->
        <activity
            android:name=".AddRestaurantActivity"
            android:exported="false" /> <!-- ✏️ Edit restaurant activity -->
        <activity
            android:name=".EditRestaurantActivity"
            android:exported="false" />
    </application>

</manifest>