<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CDMXGourmet" parent="Theme.Material3.DayNight.NoActionBar">
            <!-- Cambiar color de fondo del Toolbar -->
            <item name="colorPrimary">#619ce1</item>
            <item name="colorOnPrimary">#000000</item> <!-- negro para que el texto se lea -->
    </style>

    <style name="Theme.CDMXGourmet" parent="Base.Theme.CDMXGourmet">
        <item name="toolbarStyle">@style/WhiteToolbarStyle</item>
    </style>

    <style name="WhiteToolbarStyle" parent="Widget.AppCompat.Toolbar">
        <item name="titleTextColor">@android:color/white</item>
    </style>
</resources>